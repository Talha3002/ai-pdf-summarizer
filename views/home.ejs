<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Home - PDF Chat</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding-top: 60px; /* Accounts for fixed navbar */
            height: 100vh;
            background-color: #121826;
            color: #E0E0E0;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            overflow-x: hidden;
        }
        .main-container {
            max-width: 800px;
            text-align: center;
            padding: 20px;
        }
        .home-header {
            font-size: 36px;
            font-weight: bold;
            color: #A4C8FF;
            margin-bottom: 20px;
        }
        .description {
            font-size: 18px;
            color: #A0A6B1;
            line-height: 1.5;
            margin-bottom: 40px;
        }
        .button {
            padding: 12px 24px;
            background-color: #3A4A69;
            color: #E0E0E0;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s ease;
        }
        .button:hover {
            border: 2px solid lightblue;
            color: lightblue;
        }
    </style>
</head>
<body>
    <%- include('./partials/nav') %>

    <div class="main-container">
        <div class="home-header">Welcome to PDF Summarizer</div>
        <div class="description">
            This platform allows you to upload PDF files and interact with their content. You can ask questions, and the
            system will provide answers based on the uploaded files.
        </div>
        <a href="/user/chatwithpdf">
            <button class="button">Get Started</button>
        </a>
    </div>

    <%- include('./partials/script') %>
</body>
</html>
